import { IRtcPeerDescription } from "../model/IRtcPeerDescription";
import { Uuid } from "../model/Uuid";
import { IsoDate } from "./IsoDate";
export interface IRtcSession {
    id: Uuid;
    organizationId: Uuid;
    offer: IRtcPeerDescription;
    answer: IRtcPeerDescription;
    created: IsoDate;
}
